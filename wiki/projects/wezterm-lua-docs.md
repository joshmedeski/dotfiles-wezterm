# WezTerm & Lua Documentation

Small, focused reference docs stored as markdown files in the Obsidian vault covering how WezTerm configuration works and linking to specific features.

- Overview of how the WezTerm config table works
- How modules apply settings via `apply_to_config(config)`
- Links to specific WezTerm API features used in development
- Keep docs concise and stored as `.md` files in the wiki
